{
  "name": "raven_example",
  "version": "0.1.0",
  "description": "An example page for Raven tool.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "netlify": "npm install && rm -rf dist && mkdir dist && rsync -rv * dist --exclude ./dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@dvo/raven": "^3.0.0"
  }
}
